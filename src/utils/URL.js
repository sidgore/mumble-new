export const url = "https://mumblify.herokuapp.com";

export const loginURL = `${url}/users/login`;
export const sigupURL = `${url}/users`;
export const getAllMumbles = `${url}/getAllMumbles`;
export const postNewMumble = `${url}/mumbles`;

export const apiConfig = {
  baseUrl: url,
};
